name: DCO

on:
  pull_request:

permissions:
  contents: read

jobs:
  dco:
    name: Verify signed-off commits
    runs-on: ubuntu-latest
    steps:
      - name: DCO check
        uses: christophebedard/dco-check@0.5.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
